name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:  # 手動実行も可能

jobs:
  # ──────────────────────────────────────────────
  # WordPress テーマ SFTP デプロイ
  # ──────────────────────────────────────────────
  deploy-theme:
    name: Deploy WordPress Theme
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "${{ secrets.SFTP_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          echo "=== Key fingerprint ==="
          ssh-keygen -l -f ~/.ssh/deploy_key || true
          echo "=== ssh-keyscan wellnesshack.sakura.ne.jp ==="
          ssh-keyscan -v -p 22 -H wellnesshack.sakura.ne.jp >> ~/.ssh/known_hosts 2>&1 || true
          echo "=== known_hosts lines: $(wc -l < ~/.ssh/known_hosts) ==="

      - name: Test SSH connection
        run: |
          echo "=== Testing wellnesshack.sakura.ne.jp ==="
          ssh -v \
            -i ~/.ssh/deploy_key \
            -o StrictHostKeyChecking=no \
            -o ConnectTimeout=15 \
            "${{ secrets.SFTP_USER }}@wellnesshack.sakura.ne.jp" \
            "echo SSH_OK && pwd" 2>&1 || echo "SSH failed with code $?"

      - name: Deploy theme via rsync
        run: |
          rsync -avzr --delete \
            --exclude='wp-config.php' \
            --exclude='wp-config-security.php' \
            --exclude='.git/' \
            --exclude='.github/' \
            --exclude='node_modules/' \
            -e "ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no -o ConnectTimeout=15" \
            hachi-wp-secure/ \
            "${{ secrets.SFTP_USER }}@wellnesshack.sakura.ne.jp:${{ secrets.SFTP_REMOTE_PATH }}"

      - name: Notify Slack — Deploy success
        if: success()
        run: |
          curl -s -X POST "${{ secrets.SLACK_WEBHOOK_URL }}" \
            -H "Content-Type: application/json" \
            -d '{
              "text": "🚀 *HACHI-web* theme deployed successfully",
              "attachments": [{
                "color": "#36a64f",
                "fields": [
                  {"title": "Commit", "value": "`${{ github.sha }}`", "short": true},
                  {"title": "Branch", "value": "`${{ github.ref_name }}`", "short": true}
                ]
              }]
            }'

      - name: Notify Slack — Deploy failure
        if: failure()
        run: |
          curl -s -X POST "${{ secrets.SLACK_WEBHOOK_URL }}" \
            -H "Content-Type: application/json" \
            -d '{"text":"❌ *HACHI-web* deploy FAILED — <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View logs>"}'
